---
phase: 03-character-storyboard-workflow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/app/services/approval_gate.py
  - backend/app/services/shot_binding.py
  - backend/app/orchestration/nodes.py
  - backend/app/agents/storyboard_artist.py
  - backend/app/agents/video_generator.py
  - backend/tests/test_orchestration/test_phase3_graph.py
autonomous: true
requirements: [CHAR-02, SHOT-01]
must_haves:
  truths:
    - "Storyboard generation uses only the characters explicitly bound to the current shot."
    - "Video generation does not start until every required storyboard shot for the run is approved."
    - "The approval gate resumes on the same durable thread once the blocked shots are approved."
  artifacts:
    - path: "backend/app/services/shot_binding.py"
      provides: "Helpers that resolve shot-bound approved characters"
    - path: "backend/app/services/approval_gate.py"
      provides: "Helpers that decide whether storyboard/video can proceed"
    - path: "backend/app/orchestration/nodes.py"
      provides: "Storyboard approval gate and clip routing logic"
    - path: "backend/app/agents/storyboard_artist.py"
      provides: "Storyboard prompt construction from shot-bound cast"
    - path: "backend/app/agents/video_generator.py"
      provides: "Video prompt construction from the frozen shot contract"
    - path: "backend/tests/test_orchestration/test_phase3_graph.py"
      provides: "Regression tests for cast binding and approval gating"
  key_links:
    - from: "backend/app/services/shot_binding.py"
      to: "backend/app/agents/storyboard_artist.py"
      via: "shot-bound character resolution feeds prompt building"
      pattern: "build_character_context|ShotCharacter"
    - from: "backend/app/services/approval_gate.py"
      to: "backend/app/orchestration/nodes.py"
      via: "storyboard approval node routes to review until all required shots are approved"
      pattern: "route_stage|review_requested"
    - from: "backend/app/agents/video_generator.py"
      to: "backend/app/services/shot_binding.py"
      via: "video prompt uses the frozen approved cast instead of the project cast"
      pattern: "build_character_context"
---

<objective>
Make storyboard and video generation consume shot-bound approved characters and hard-stop before clip generation until the storyboard set is fully approved.

Purpose: Phase 3 must preserve character identity and enforce a single linear storyboard-to-video gate.
Output: Binding helpers, approval gating, updated agents, and orchestration regression tests.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-character-storyboard-workflow/03-CONTEXT.md
@.planning/phases/03-character-storyboard-workflow/03-RESEARCH.md
@.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
@backend/app/orchestration/nodes.py
@backend/app/agents/storyboard_artist.py
@backend/app/agents/video_generator.py
@backend/app/services/run_recovery.py

<interfaces>
Key runtime hooks to preserve:

From `backend/app/orchestration/nodes.py`
```python
async def storyboard_approval_node(state, runtime) -> dict[str, Any]
def route_after_storyboard_approval(state) -> str
```

From `backend/app/agents/storyboard_artist.py`
```python
def _build_image_prompt(self, shot: Shot, characters: list[Character], *, style: str) -> str
```

From `backend/app/agents/video_generator.py`
```python
def _build_video_prompt(self, shot: Shot, characters: list[Character], *, style: str) -> str
```
</interfaces>
</execution_context>

<context>
The new gate must be enforced server-side. Do not rely on the browser to decide when clip generation is allowed.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the shot-binding and gate behavior in tests</name>
  <read_first>
    - @backend/app/orchestration/nodes.py
    - @backend/app/agents/storyboard_artist.py
    - @backend/app/agents/video_generator.py
    - @backend/app/services/run_recovery.py
    - @backend/app/models/project.py
    - @.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
  </read_first>
  <files>backend/tests/test_orchestration/test_phase3_graph.py</files>
  <action>Write failing orchestration tests that prove two things: storyboard and video prompt building use only the shot-bound approved character set, and clip generation stays blocked until every required storyboard shot is approved for the current run.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_orchestration/test_phase3_graph.py -q</automated>
  </verify>
  <acceptance_criteria>The new orchestration tests exist, fail before implementation, and describe the approved-shot gate and shot-bound cast contract precisely.</acceptance_criteria>
  <done>The graph and agent contract are pinned by failing regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable shot-binding and gate helpers</name>
  <read_first>
    - @backend/app/models/project.py
    - @backend/app/schemas/project.py
    - @backend/app/orchestration/nodes.py
    - @backend/app/agents/storyboard_artist.py
    - @backend/app/agents/video_generator.py
  </read_first>
  <files>
    backend/app/services/approval_gate.py
    backend/app/services/shot_binding.py
  </files>
  <action>Create the reusable helpers that resolve the approved shot-bound character set and determine whether a run is allowed to enter clip generation. Keep the helpers database-driven and project-scoped so the agents never fall back to the whole project cast by default.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_orchestration/test_phase3_graph.py -q</automated>
  </verify>
  <acceptance_criteria>The helper layer can answer both questions required by the phase: which characters belong to the current shot, and whether all required storyboard shots are approved.</acceptance_criteria>
  <done>Reusable binding and approval-gate helpers exist for the orchestration layer.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the approval gate in orchestration and agents</name>
  <read_first>
    - @backend/app/services/approval_gate.py
    - @backend/app/services/shot_binding.py
    - @backend/app/orchestration/nodes.py
    - @backend/app/agents/storyboard_artist.py
    - @backend/app/agents/video_generator.py
  </read_first>
  <files>
    backend/app/orchestration/nodes.py
    backend/app/agents/storyboard_artist.py
    backend/app/agents/video_generator.py
  </files>
  <action>Update the storyboard approval node to route back to review while any required shot remains unapproved, and update the storyboard/video agents to build prompts from the frozen shot-bound cast instead of the full project cast. Keep the current durable thread and resume semantics intact.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_orchestration/test_phase3_graph.py -q</automated>
  </verify>
  <acceptance_criteria>Storyboard generation, clip routing, and video prompt construction all honor the approved shot contract and the graph still resumes on the same durable run/thread.</acceptance_criteria>
  <done>The orchestration path enforces the shot gate and consumes only shot-bound approved characters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime state → generation agents | Approved shot selection must come from the server, not mutable client state. |
| database → orchestration graph | The graph must decide from persisted approval state, not ephemeral UI assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | shot binding helpers | mitigate | Resolve shot-bound characters from persisted relations or frozen snapshots at runtime. |
| T-03-05 | D | storyboard/video gate | mitigate | Block clip generation until all required shots are approved, rather than allowing partial execution. |
| T-03-06 | R | graph routing | mitigate | Use a server-authored approval decision path so the resume trail matches the stored run state. |
</threat_model>

<verification>
Run the orchestration regression slice after each task, and finish with the same slice plus any backend approval tests needed to confirm the gate still aligns with the review contract from Plan 01.
</verification>

<success_criteria>
- Storyboard/video prompt construction uses only shot-bound approved characters.
- Video generation cannot start until every required storyboard shot is approved.
- The graph resumes on the same durable thread after approval without forking the run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-storyboard-workflow/03-02-SUMMARY.md`.
</output>
