---
phase: 03-character-storyboard-workflow
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/app/types/index.ts
  - frontend/app/services/api.ts
  - frontend/app/stores/editorStore.ts
  - frontend/app/hooks/useWebSocket.ts
  - frontend/app/components/canvas/canvasEvents.ts
  - frontend/app/components/canvas/shapes/types.ts
autonomous: true
requirements: [CHAR-01, SHOT-01]
must_haves:
  truths:
    - "Frontend types can represent the current approval state for characters and shots."
    - "WebSocket payloads can hydrate the store with approved/superseded review state without breaking existing event names."
    - "Canvas events can express approve actions separately from edit/regenerate actions."
  artifacts:
    - path: "frontend/app/types/index.ts"
      provides: "Typed review state for Character, Shot, and websocket payloads"
    - path: "frontend/app/services/api.ts"
      provides: "Approve/update calls for characters and shots"
    - path: "frontend/app/stores/editorStore.ts"
      provides: "Client-side review state cache"
    - path: "frontend/app/hooks/useWebSocket.ts"
      provides: "WS hydration for approval state"
    - path: "frontend/app/components/canvas/canvasEvents.ts"
      provides: "Typed approve events from canvas cards"
    - path: "frontend/app/components/canvas/shapes/types.ts"
      provides: "Shape contracts that carry the updated Character/Shot review metadata"
  key_links:
    - from: "backend/app/schemas/ws.py"
      to: "frontend/app/hooks/useWebSocket.ts"
      via: "same event names with richer approval payloads"
      pattern: "character_updated|shot_updated"
    - from: "frontend/app/services/api.ts"
      to: "frontend/app/stores/editorStore.ts"
      via: "approve/update mutations replace the cached review state"
      pattern: "approve|updateCharacter|updateShot"
    - from: "frontend/app/components/canvas/canvasEvents.ts"
      to: "frontend/app/components/canvas/InfiniteCanvas.tsx"
      via: "typed approve events emitted by canvas controls"
      pattern: "approve-character|approve-shot"
---

<objective>
Teach the frontend about approval state so the canvas and store can surface the backend review contract without inventing a new workflow shell.

Purpose: The UI must be able to render and mutate the server-authored approval state before the per-card controls are added.
Output: Typed review contracts, API helpers, store hydration, websocket parsing, and typed canvas events.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-character-storyboard-workflow/03-CONTEXT.md
@.planning/phases/03-character-storyboard-workflow/03-RESEARCH.md
@.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
@frontend/app/types/index.ts
@frontend/app/services/api.ts
@frontend/app/stores/editorStore.ts
@frontend/app/hooks/useWebSocket.ts
@frontend/app/components/canvas/canvasEvents.ts

<interfaces>
Current frontend state hooks to extend:

From `frontend/app/types/index.ts`
```typescript
export interface Character { id: number; project_id: number; name: string; description: string | null; image_url: string | null; }
export interface Shot { id: number; project_id: number; order: number; description: string; prompt: string | null; image_prompt: string | null; image_url: string | null; video_url: string | null; duration: number | null; }
export interface RecoveryControlRead { ... }
export type WsEventType = ... | "character_updated" | "shot_updated" | ...
```

From `frontend/app/stores/editorStore.ts`
```typescript
setCharacters(characters)
setShots(shots)
updateCharacter(character)
updateShot(shot)
```

From `frontend/app/components/canvas/canvasEvents.ts`
```typescript
export interface CanvasEvents {
  "edit-character": Character;
  "regenerate-character": number;
  "edit-shot": Shot;
  "regenerate-shot": { id: number; type: "image" | "video" };
}
```
</interfaces>
</execution_context>

<context>
The frontend should only understand the current approval state, not a version browser. Keep the UI contract small and server-driven.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the frontend review contracts in tests</name>
  <read_first>
    - @frontend/app/types/index.ts
    - @frontend/app/services/api.ts
    - @frontend/app/stores/editorStore.ts
    - @frontend/app/hooks/useWebSocket.ts
    - @frontend/app/components/canvas/canvasEvents.ts
    - @.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
  </read_first>
  <files>
    frontend/app/stores/editorStore.test.ts
    frontend/app/hooks/useWebSocket.test.ts
    frontend/app/components/canvas/canvasEvents.test.ts
  </files>
  <action>Write failing frontend tests that describe the new review contract: the store must accept approved/superseded character and shot states, websocket events must hydrate those states, and the approve/update API helpers must expose the new review endpoints without breaking the existing project/shot/character fetch calls.</action>
  <verify>
    <automated>pnpm test -- --run frontend/app/stores/editorStore.test.ts frontend/app/hooks/useWebSocket.test.ts frontend/app/components/canvas/canvasEvents.test.ts</automated>
  </verify>
  <acceptance_criteria>The tests exist, fail before implementation, and pin the state-shape contract for the review flow.</acceptance_criteria>
  <done>The frontend review contract is captured in failing regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend shared review types and API/store wiring</name>
  <read_first>
    - @backend/app/schemas/project.py
    - @backend/app/schemas/ws.py
    - @frontend/app/types/index.ts
    - @frontend/app/services/api.ts
    - @frontend/app/stores/editorStore.ts
    - @frontend/app/hooks/useWebSocket.ts
  </read_first>
  <files>
    frontend/app/types/index.ts
    frontend/app/services/api.ts
    frontend/app/stores/editorStore.ts
    frontend/app/hooks/useWebSocket.ts
  </files>
  <action>Add the typed approval state to Character, Shot, websocket payloads, and the editor store. Add approve/update API helpers for characters and shots, and ensure websocket hydration updates the cached review state instead of discarding it. Keep event names stable and keep the API surface aligned with the backend contracts from Plan 01.</action>
  <verify>
    <automated>pnpm test -- --run frontend/app/stores/editorStore.test.ts frontend/app/hooks/useWebSocket.test.ts</automated>
  </verify>
  <acceptance_criteria>The shared frontend contract can express current approval state, and the store/websocket path can preserve that state across refreshes and live updates.</acceptance_criteria>
  <done>The frontend shared state and API layers understand the approval contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add typed approve events for canvas controls</name>
  <read_first>
    - @frontend/app/components/canvas/canvasEvents.ts
    - @frontend/app/components/canvas/shapes/types.ts
    - @frontend/app/components/canvas/CharacterSectionShape.tsx
    - @frontend/app/components/canvas/StoryboardSectionShape.tsx
  </read_first>
  <files>
    frontend/app/components/canvas/canvasEvents.ts
    frontend/app/components/canvas/shapes/types.ts
  </files>
  <action>Extend the canvas event bus and shape prop contracts with explicit approve actions so the later UI pass can trigger character and shot approval without overloading edit/regenerate events.</action>
  <verify>
    <automated>pnpm test -- --run frontend/app/stores/editorStore.test.ts frontend/app/hooks/useWebSocket.test.ts</automated>
  </verify>
  <acceptance_criteria>The canvas event layer can emit explicit approval actions and the shape contracts can carry the updated review metadata cleanly.</acceptance_criteria>
  <done>The canvas event bus and shape contracts can carry explicit approval actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| websocket → store | Approval data from the server must not be mutated into a false approved state by the client. |
| UI actions → API | Canvas actions must call typed API helpers instead of fabricating review state locally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | editor store | mitigate | Treat the store as a cache of server-authored approval state, not the source of truth. |
| T-03-08 | S | approve mutations | mitigate | Route approve actions through typed API helpers that match the backend review contract. |
| T-03-09 | I | websocket hydration | mitigate | Hydrate only project-scoped review payloads and preserve the existing event names. |
</threat_model>

<verification>
Run the focused frontend unit tests after each task, then finish with the same test slice plus `pnpm tsc --noEmit` once the UI pass from Plan 04 is complete.
</verification>

<success_criteria>
- Frontend state can represent approved/superseded character and shot review data.
- WebSocket updates hydrate the review state without breaking the current event contract.
- Canvas controls can emit typed approve actions for later UI wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-storyboard-workflow/03-03-SUMMARY.md`.
</output>
