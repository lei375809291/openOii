---
phase: 02-langgraph-orchestration-durable-execution
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - frontend/app/hooks/useWebSocket.ts
  - frontend/app/stores/editorStore.ts
  - frontend/app/pages/ProjectPage.tsx
  - frontend/app/services/api.ts
  - frontend/app/types/index.ts
autonomous: true
requirements:
  - PIPE-01
  - REL-01
must_haves:
  truths:
    - "A creator can see a recovery summary in the project page before a resumable run continues."
    - "A creator can choose resume or cancel from the same project page when a run is already active."
    - "A creator can still follow progress and approval-gate updates while the recovery controls are visible."
  artifacts:
    - path: frontend/app/hooks/useWebSocket.ts
      provides: "typed recovery and gate event handling"
    - path: frontend/app/stores/editorStore.ts
      provides: "recovery-summary and active-run UI state"
    - path: frontend/app/pages/ProjectPage.tsx
      provides: "recovery banner and resume/cancel controls"
  key_links:
    - from: frontend/app/hooks/useWebSocket.ts
      to: frontend/app/stores/editorStore.ts
      via: "recovery and gate event dispatch"
      pattern: "run_awaiting_confirm|run_confirmed|run_progress"
    - from: frontend/app/stores/editorStore.ts
      to: frontend/app/pages/ProjectPage.tsx
      via: "recovery banner state"
      pattern: "recovery|resume|cancel"
---

# Phase 2 / Plan 04 — Frontend recovery and active-run UI

> **For agentic workers:** REQUIRED: Use `superpowers:subagent-driven-development` if subagents are available, or `superpowers:executing-plans` to implement this plan.

**Goal:** Surface the backend recovery summary and active-run choice in the creator UI.

**Architecture:** Thread the server-authored recovery payload through the existing websocket store into the project page, then render the resume/cancel choice without changing the rest of the progress surface. Keep this plan frontend-only so the backend contract in Plan 03 stays isolated and easy to validate.

**Tech Stack:** React, Zustand, TypeScript, existing project page UI.

---

<objective>
Show creators where a resumable run stopped and let them decide what to do from the project page.

Purpose: make resume/cancel actions explicit without hiding the existing progress feedback.
Output: updated websocket handling, store state, and project page recovery controls.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/schemas/ws.py
@frontend/app/hooks/useWebSocket.ts
@frontend/app/stores/editorStore.ts
@frontend/app/pages/ProjectPage.tsx
@frontend/app/services/api.ts
@frontend/app/types/index.ts
</execution_context>

<context>
<interfaces>
- The backend already emits typed recovery and gate payloads by this point in the phase.
- `useWebSocket.ts` is the single ingress point for run progress and confirmation events.
- `editorStore.ts` is the shared state layer for run/recovery UI.
- `ProjectPage.tsx` is the creator-facing surface that should render the recovery banner and actions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread recovery state through the websocket store</name>
  <read_first>
    - frontend/app/hooks/useWebSocket.ts
    - frontend/app/stores/editorStore.ts
    - frontend/app/types/index.ts
    - backend/app/schemas/ws.py
  </read_first>
  <files>
    frontend/app/hooks/useWebSocket.ts
    frontend/app/stores/editorStore.ts
    frontend/app/types/index.ts
  </files>
  <acceptance_criteria>
    - Recovery and gate payloads are represented in typed frontend state.
    - Existing run-progress behavior still works unchanged.
    - The store exposes enough data for the project page to render resume/cancel choices.
  </acceptance_criteria>
  <action>Extend the websocket handling and editor store so recovery summary, current stage, preserved stages, and active-run status become explicit frontend state. Keep the existing progress contract intact while adding the minimal shape needed for the new recovery banner.</action>
  <verify>
    <automated>pnpm --dir frontend tsc --noEmit</automated>
  </verify>
  <done>Frontend state can represent recovery and active-run control without breaking existing websocket consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Render the recovery banner and controls</name>
  <read_first>
    - frontend/app/stores/editorStore.ts
    - frontend/app/pages/ProjectPage.tsx
    - frontend/app/services/api.ts
    - frontend/app/types/index.ts
  </read_first>
  <files>
    frontend/app/pages/ProjectPage.tsx
    frontend/app/services/api.ts
  </files>
  <acceptance_criteria>
    - The project page shows a recovery summary before a resumable run continues.
    - The creator can choose resume or cancel from the same page.
    - Progress and approval-gate updates remain visible while the recovery banner is shown.
  </acceptance_criteria>
  <action>Render the recovery banner on the project page using the store data from Task 1 and wire the resume/cancel actions to the existing API surface. Keep the UI focused on the creator decision and do not introduce a new recovery workflow outside the current project page.</action>
  <verify>
    <automated>pnpm --dir frontend test && pnpm --dir frontend tsc --noEmit</automated>
  </verify>
  <done>The creator can see and act on recovery state directly in the existing project UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| websocket payloads → frontend store | The store must only accept typed recovery state from the backend. |
| frontend UI → resume/cancel API | User actions must remain explicit and bound to the active project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | resume/cancel UI actions | mitigate | Keep the API calls explicit and project-scoped; never infer target run from stale local state. |
| T-02-11 | Tampering | frontend recovery store | mitigate | Derive store state only from validated websocket payloads and typed API responses. |
| T-02-12 | Information Disclosure | recovery banner contents | accept | The banner only displays creator-visible run status and stage summary, not secrets. |
</threat_model>

<verification>
[Overall phase checks]
- `pnpm --dir frontend test && pnpm --dir frontend tsc --noEmit`
- The project page renders recovery controls and progress in the same surface.
</verification>

<success_criteria>
Creators can see the resumable-run summary and choose resume or cancel directly in the project page while progress remains visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-orchestration-durable-execution/02-04-SUMMARY.md`.
</output>
