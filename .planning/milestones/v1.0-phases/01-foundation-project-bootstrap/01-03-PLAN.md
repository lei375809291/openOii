---
phase: 01-foundation-project-bootstrap
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/0001_phase1_bootstrap.py
  - backend/tests/test_migrations.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
must_haves:
  truths:
    - "Alembic can target the SQLModel metadata and generate the Phase 1 schema from scratch"
    - "The migration path covers the existing bootstrap tables plus the new Run/Stage/Artifact lineage tables"
    - "A migration smoke test proves upgrade head works on a freshly provisioned blank database"
  artifacts:
    - path: "backend/alembic/env.py"
      provides: "Metadata wiring and database URL bootstrap"
      contains: "target_metadata"
    - path: "backend/alembic/versions/0001_phase1_bootstrap.py"
      provides: "Initial Phase 1 schema revision"
      contains: "revision ="
    - path: "backend/tests/test_migrations.py"
      provides: "Alembic smoke coverage"
      min_lines: 40
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/db/session.py"
      via: "SQLModel metadata import path"
      pattern: "target_metadata"
    - from: "backend/alembic/versions/0001_phase1_bootstrap.py"
      to: "backend/app/models/*"
      via: "generated schema for existing + new tables"
      pattern: 'op\.create_table'
    - from: "backend/tests/test_migrations.py"
      to: "backend/alembic/env.py"
      via: "upgrade/downgrade smoke test"
      pattern: "alembic upgrade head"
---

<objective>
Add Alembic as the canonical schema evolution path for Phase 1.

Purpose: stop relying on permanent `create_all` thinking and prove the schema can be reproduced and migrated from metadata.
Output: an Alembic environment, an initial Phase 1 revision, and a smoke test that exercises upgrade head.
</objective>

<execution_context>
@/home/xeron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/xeron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-bootstrap/01-RESEARCH.md
@backend/app/db/session.py
@backend/app/models/project.py
@backend/app/models/agent_run.py
@backend/app/models/run.py
@backend/app/models/stage.py
@backend/app/models/artifact.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the migration smoke test first</name>
  <files>backend/tests/test_migrations.py</files>
  <behavior>
    - Add a failing smoke test that provisions a fresh empty database file in an isolated temp directory, runs `alembic upgrade head` against that blank database, and asserts the resulting metadata includes the Phase 1 tables.
    - Assert that the migration path includes both the existing bootstrap models and the new lineage tables.
    - Keep the smoke test isolated; do not make it depend on the full app runtime or the manual orchestrator.
  </behavior>
  <action>Define the migration expectations before authoring the Alembic files so drift is obvious. The smoke test should create a blank temp database for each run, remove any stale file before upgrade, and prove the schema can be recreated from scratch, not just that the app starts.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_migrations.py -x</automated>
  </verify>
  <done>The migration smoke test fails for the right reason before Alembic is wired and becomes the acceptance gate for the revision files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Alembic to SQLModel metadata</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/0001_phase1_bootstrap.py</files>
  <behavior>
    - Create the Alembic environment, point `target_metadata` at the SQLModel registry, and generate the initial Phase 1 schema revision.
    - Include the current Project/AgentRun/Message/ConfigItem tables plus the new Run/Stage/Artifact lineage tables in the bootstrap revision.
    - Keep the runtime bootstrap unchanged in Phase 1; do not replace `create_all` startup behavior yet, because that belongs to a later hardening pass.
  </behavior>
  <action>Implement the Alembic path so the smoke test from Task 1 passes. Generate the initial revision from metadata and keep the migration story self-contained.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_migrations.py -x &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <done>The backend has a working Alembic bootstrap and the schema can be upgraded cleanly on a blank database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic metadata bootstrap → database schema | Migration files must match the SQLModel registry exactly |
| migration runner → operator environment | Alembic reads the database URL from environment/config |
| revision script → schema history | Incorrect revision content can silently drift from the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | migration revision contents | mitigate | Generate the initial revision from SQLModel metadata and lock it with an automated smoke test. |
| T-01-06 | R | migration history | mitigate | Keep the revision file in git and fail the smoke test on drift so schema changes are attributable. |
| T-01-07 | I | Alembic env config | accept | The environment already needs database configuration; do not encode secrets in revision files. |
</threat_model>

<verification>
  The smoke test and `alembic upgrade head` must succeed on a freshly provisioned blank database before the plan is considered complete. The test itself is responsible for creating/resetting that disposable database path.
  </verification>

<success_criteria>
- Alembic is configured under backend/alembic/ and uses SQLModel metadata as source of truth.
- The initial Phase 1 revision recreates the current schema plus the new lineage tables.
- The migration smoke test passes on an isolated database.
- Phase 1 still leaves runtime bootstrap behavior untouched unless a later phase explicitly changes it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-bootstrap/01-03-SUMMARY.md`
</output>

<commit_strategy>
- Commit 1: add migration smoke coverage only (`backend/tests/test_migrations.py`).
- Commit 2: add Alembic environment and initial revision (`backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_phase1_bootstrap.py`).
</commit_strategy>
