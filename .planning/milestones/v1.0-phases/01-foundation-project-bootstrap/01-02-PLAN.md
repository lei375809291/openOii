---
phase: 01-foundation-project-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/app/orchestration/__init__.py
  - backend/app/orchestration/state.py
  - backend/app/orchestration/graph.py
  - backend/app/orchestration/runtime.py
  - backend/tests/test_orchestration/test_phase1_graph.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
must_haves:
  truths:
    - "A LangGraph StateGraph compiles from a dedicated backend orchestration package"
    - "The graph state carries project_id and run_id explicitly, and Run.thread_id is the persisted canonical LangGraph thread identity"
    - "Mergeable state uses reducers instead of ad hoc mutable dict updates"
  artifacts:
    - path: "backend/app/models/run.py"
      provides: "Persisted LangGraph thread_id source of truth"
      contains: "thread_id"
    - path: "backend/app/orchestration/state.py"
      provides: "Typed graph state schema and reducers"
      contains: "TypedDict"
    - path: "backend/app/orchestration/graph.py"
      provides: "Compileable LangGraph skeleton"
      contains: "StateGraph"
    - path: "backend/app/orchestration/runtime.py"
      provides: "Graph invocation config helper"
      contains: "thread_id"
    - path: "backend/tests/test_orchestration/test_phase1_graph.py"
      provides: "Compilation and state-shape coverage"
      min_lines: 50
  key_links:
    - from: "backend/app/orchestration/state.py"
      to: "backend/app/orchestration/graph.py"
      via: "StateGraph generic state contract"
      pattern: "Annotated|TypedDict"
    - from: "backend/app/orchestration/runtime.py"
      to: "backend/app/models/run.py"
      via: "run identity → thread_id mapping"
      pattern: "thread_id"
    - from: "backend/tests/test_orchestration/test_phase1_graph.py"
      to: "backend/app/orchestration/graph.py"
      via: "graph compilation and state contract assertions"
      pattern: 'compile\('
---

<objective>
Create the thin LangGraph boundary for Phase 1 without turning it into the durable execution runtime.

Purpose: establish the orchestration home, state schema, and ID mapping now so Phase 2 can add persistence and interrupts without reshaping the architecture.
Output: a compileable graph package, a runtime config helper, and tests that pin the state contract.
</objective>

<execution_context>
@/home/xeron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/xeron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-bootstrap/01-RESEARCH.md
@backend/app/agents/orchestrator.py
@backend/app/api/v1/routes/generation.py
@backend/app/models/run.py
@backend/app/models/stage.py
@backend/app/models/artifact.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the graph contract</name>
  <files>backend/tests/test_orchestration/test_phase1_graph.py, backend/app/orchestration/state.py, backend/app/orchestration/runtime.py</files>
  <behavior>
    - Write failing tests for the graph state shape, including the reducer-backed fields and the explicit project_id/run_id/thread_id mapping.
    - Define the typed state contract and the runtime helper signature before the graph nodes exist.
    - Make the ownership decision explicit: Run.thread_id is stored on the Run row, and the runtime helper derives the LangGraph invocation config from that persisted value.
    - Keep the state small and deterministic; do not add checkpoint/resume, interrupt, or worker concerns in Phase 1.
  </behavior>
  <action>Introduce the LangGraph state schema and config helper first so downstream nodes can compile against a real contract. Use the persisted Run.thread_id value as the source of truth for LangGraph thread ownership, and do not move that ownership to Project or any later runtime cache.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_orchestration/test_phase1_graph.py -x</automated>
  </verify>
  <done>The graph contract is explicit, typed, and locked by tests before node implementation begins, with Run.thread_id clearly defined as the persisted ownership boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the compileable graph skeleton</name>
  <files>backend/app/orchestration/__init__.py, backend/app/orchestration/graph.py, backend/app/orchestration/runtime.py</files>
  <behavior>
    - Implement a compileable StateGraph with START/END and minimal node stubs that only prove the Phase 1 boundary exists.
    - Expose a single entry point for future phases to invoke the graph with the runtime config helper.
    - Explicitly defer durable checkpoints, replay, interrupt/resume, and queued heavy workers to Phase 2.
  </behavior>
  <action>Implement the orchestration shell so the tests from Task 1 pass and the package can be imported cleanly. Keep node behavior deterministic and side-effect light; this is a boundary, not the full pipeline.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_orchestration/test_phase1_graph.py -x</automated>
  </verify>
  <done>The LangGraph package compiles, exports the Phase 1 shell, and still leaves durable runtime behavior for later phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph input → node state | Untrusted project/run data becomes graph state |
| node state → downstream orchestration helpers | Reducer merges must remain deterministic |
| orchestration package → future runtime | Phase 1 shell must not leak secrets or implicit side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | T | graph state contract | mitigate | Keep state fields explicit and typed; reducer functions must be pure and tested. |
| T-01-03 | I | runtime config helper | mitigate | Derive thread_id from run identity only and avoid storing secrets or provider keys in the graph state. |
| T-01-04 | D | compileable graph skeleton | accept | Phase 1 intentionally does not implement durable retries or workers; the shell stays small to reduce failure surface. |
</threat_model>

<verification>
  The graph package must compile and the state-contract tests must pass without importing the legacy manual orchestrator as a dependency. Run.thread_id must remain the only persisted thread ownership field for Phase 1.
</verification>

<success_criteria>
- The backend has a dedicated LangGraph home under backend/app/orchestration/.
- The graph state includes project_id, run_id, and thread_id explicitly.
- The graph compiles and is covered by a focused orchestration test.
- Phase 2 concerns remain deferred instead of sneaking into the shell.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-bootstrap/01-02-SUMMARY.md`
</output>

<commit_strategy>
- Commit 1: add graph-contract tests and typed state/runtime contract (`backend/tests/test_orchestration/test_phase1_graph.py`, `backend/app/orchestration/state.py`, `backend/app/orchestration/runtime.py`).
- Commit 2: add the compileable graph shell and exports (`backend/app/orchestration/__init__.py`, `backend/app/orchestration/graph.py`, plus any runtime helper adjustments needed to satisfy the tests).
</commit_strategy>
