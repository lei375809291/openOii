---
phase: 03-character-storyboard-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/project.py
  - backend/app/schemas/project.py
  - backend/app/api/v1/routes/characters.py
  - backend/app/api/v1/routes/shots.py
  - backend/app/schemas/ws.py
  - backend/alembic/versions/0002_phase3_approval_workflow.py
  - backend/tests/test_api/test_character_storyboard_review.py
autonomous: true
requirements: [CHAR-01, SHOT-01]
must_haves:
  truths:
    - "Creator can edit character references with name, description, and optional primary image before approval."
    - "Creator can approve a character and the frozen approved reference stays stable for later stages in the same run."
    - "Creator can edit a storyboard shot's bound cast and structured intent before approval."
    - "Approved shots expose the current approved/superseded state without exposing a version browser."
  artifacts:
    - path: "backend/app/models/project.py"
      provides: "Character/Shot review fields plus shot-character binding model"
    - path: "backend/app/schemas/project.py"
      provides: "CharacterRead/ShotRead approval payloads and update contracts"
    - path: "backend/alembic/versions/0002_phase3_approval_workflow.py"
      provides: "Migration for approval and binding columns"
    - path: "backend/app/api/v1/routes/characters.py"
      provides: "Character approve/update endpoints"
    - path: "backend/app/api/v1/routes/shots.py"
      provides: "Shot approve/update endpoints"
    - path: "backend/app/schemas/ws.py"
      provides: "Typed websocket payloads carrying current approval state"
  key_links:
    - from: "backend/app/api/v1/routes/characters.py"
      to: "backend/app/models/project.py"
      via: "approve endpoint freezes the current character snapshot"
      pattern: "character_updated|approved"
    - from: "backend/app/api/v1/routes/shots.py"
      to: "backend/app/models/project.py"
      via: "shot approve endpoint freezes cast, duration, camera, and motion note"
      pattern: "shot_updated|approved"
    - from: "backend/app/schemas/ws.py"
      to: "frontend/app/hooks/useWebSocket.ts"
      via: "same event names with richer payloads"
      pattern: "character_updated|shot_updated"
---

<objective>
Persist approval state for characters and storyboard shots so downstream stages can trust frozen references.

Purpose: Phase 3 needs a server-authored review contract before any orchestration or UI work can safely depend on it.
Output: Approval-aware models, schemas, routes, websocket payloads, and backend tests.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-character-storyboard-workflow/03-CONTEXT.md
@.planning/phases/03-character-storyboard-workflow/03-RESEARCH.md
@.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
@backend/app/models/project.py
@backend/app/schemas/project.py
@backend/app/api/v1/routes/characters.py
@backend/app/api/v1/routes/shots.py
@backend/app/schemas/ws.py

<interfaces>
Current contract shape to extend:

From `backend/app/models/project.py`
```python
class Character(SQLModel, table=True):
    id: Optional[int]
    project_id: int
    name: str
    description: Optional[str]
    image_url: Optional[str]

class Shot(SQLModel, table=True):
    id: Optional[int]
    project_id: int
    order: int
    description: str
    prompt: Optional[str]
    image_prompt: Optional[str]
    image_url: Optional[str]
    video_url: Optional[str]
    duration: Optional[float]
```

From `backend/app/schemas/project.py`
```python
class CharacterRead(BaseModel): ...
class CharacterUpdate(BaseModel): ...
class ShotRead(BaseModel): ...
class ShotUpdate(BaseModel): ...
```

From `backend/app/schemas/ws.py`
```python
class WsEvent(BaseModel):
    type: WsEventType
    data: dict[str, Any]
```
</interfaces>
</execution_context>

<context>
The schema split is at the agent's discretion, but the final contract must preserve the frozen-approved reference and shot-bound cast semantics from the phase decisions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the backend review contract in tests</name>
  <read_first>
    - @backend/app/models/project.py
    - @backend/app/schemas/project.py
    - @backend/app/api/v1/routes/characters.py
    - @backend/app/api/v1/routes/shots.py
    - @backend/app/schemas/ws.py
    - @.planning/phases/03-character-storyboard-workflow/03-VALIDATION.md
  </read_first>
  <files>backend/tests/test_api/test_character_storyboard_review.py</files>
  <action>Write failing backend tests that describe the phase contract: editable character references with replaceable primary images, per-character approval that freezes the approved snapshot, shot updates that capture bound character IDs plus duration/camera/motion note, and websocket payloads that expose only the current approval state.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_api/test_character_storyboard_review.py -q</automated>
  </verify>
  <acceptance_criteria>The new test file exists, fails before implementation, and encodes the exact approval-freeze semantics for characters and shots.</acceptance_criteria>
  <done>The backend review contract is frozen, persisted, and covered by targeted tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add approval-aware models and migration</name>
  <read_first>
    - @backend/app/models/project.py
    - @backend/app/schemas/project.py
    - @backend/tests/test_api/test_character_storyboard_review.py
    - @.planning/phases/03-character-storyboard-workflow/03-CONTEXT.md
  </read_first>
  <files>
    backend/app/models/project.py
    backend/app/schemas/project.py
    backend/alembic/versions/0002_phase3_approval_workflow.py
  </files>
  <action>Add approval/version metadata to Character and Shot, include an explicit shot-character binding model or association table, and extend the read/update schemas so approvals can freeze the current snapshot while keeping draft edits separate. Keep the contract server-owned; do not introduce any version browser UI or client-side lineage logic.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_api/test_character_storyboard_review.py -q</automated>
  </verify>
  <acceptance_criteria>A new migration can create the approval columns/tables from scratch, and the read schemas expose the current approval state plus bound shot character IDs.</acceptance_criteria>
  <done>The approval-aware backend models and migration are in place and match the test contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add approve endpoints and websocket payloads</name>
  <read_first>
    - @backend/app/api/v1/routes/characters.py
    - @backend/app/api/v1/routes/shots.py
    - @backend/app/schemas/ws.py
    - @backend/app/models/project.py
    - @backend/app/schemas/project.py
  </read_first>
  <files>
    backend/app/api/v1/routes/characters.py
    backend/app/api/v1/routes/shots.py
    backend/app/schemas/ws.py
  </files>
  <action>Add explicit approval transitions that freeze the approved character or shot snapshot, reject incomplete shot approvals until the required structured intent exists, and broadcast enriched `character_updated` / `shot_updated` events that carry only the current approved/superseded state.</action>
  <verify>
    <automated>uv run pytest backend/tests/test_api/test_character_storyboard_review.py -q</automated>
  </verify>
  <acceptance_criteria>Creators can approve characters and shots through the API, and the websocket layer reflects the resulting frozen state without changing event names.</acceptance_criteria>
  <done>The backend API can freeze review state and broadcast the current approval snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted review edits and approval requests cross here. |
| API → websocket consumers | Server-authored approval state is broadcast to the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | approval endpoints | mitigate | Validate approval transitions server-side and freeze the approved snapshot instead of trusting client state. |
| T-03-02 | I | websocket payloads | mitigate | Broadcast only project-scoped approval data and keep event names stable so the frontend cannot infer hidden lineage. |
| T-03-03 | R | review actions | mitigate | Persist the approved timestamp and frozen snapshot on the server so approval decisions are auditable. |
</threat_model>

<verification>
Run the targeted backend test slice after each task, then finish with the same slice plus the orchestration test from Plan 02 once that plan is available.
</verification>

<success_criteria>
- Character references can be edited, approved, and frozen per character.
- Storyboard shots can store shot-bound character IDs plus structured intent fields before approval.
- Backend read models and websocket payloads expose only the current approved/superseded state.
- The phase keeps version browser behavior out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-storyboard-workflow/03-01-SUMMARY.md`.
</output>
