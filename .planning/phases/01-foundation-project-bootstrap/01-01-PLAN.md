---
phase: 01-foundation-project-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_api/test_projects.py
  - backend/tests/test_api/test_config.py
  - backend/tests/test_domain/test_lineage_models.py
  - backend/app/models/run.py
  - backend/app/models/stage.py
  - backend/app/models/artifact.py
  - backend/app/db/session.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
must_haves:
  truths:
    - "Creator can create a project with title, story, and style that persists in PostgreSQL"
    - "Creator can continue using the existing text/image/video provider configuration surface without breaking masking or env/DB precedence"
    - "Canonical Run, Stage, and Artifact lineage tables exist and can be linked back to a project/run"
  artifacts:
    - path: "backend/tests/test_api/test_projects.py"
      provides: "Regression coverage for project creation persistence"
      min_lines: 40
    - path: "backend/tests/test_api/test_config.py"
      provides: "Regression coverage for provider config masking and persistence"
      min_lines: 80
    - path: "backend/tests/test_domain/test_lineage_models.py"
      provides: "Model contract coverage for Run/Stage/Artifact lineage"
      min_lines: 50
    - path: "backend/app/models/run.py"
      provides: "Canonical run table"
      contains: "class Run"
    - path: "backend/app/models/stage.py"
      provides: "Stage table"
      contains: "class Stage"
    - path: "backend/app/models/artifact.py"
      provides: "Artifact table"
      contains: "class Artifact"
    - path: "backend/app/db/session.py"
      provides: "Metadata registration for new tables"
      contains: "SQLModel.metadata.create_all"
  key_links:
    - from: "backend/tests/test_api/test_projects.py"
      to: "backend/app/api/v1/routes/projects.py"
      via: "POST /api/v1/projects contract"
      pattern: "post\\(\"/api/v1/projects\""
    - from: "backend/tests/test_api/test_config.py"
      to: "backend/app/api/v1/routes/config.py"
      via: "admin-config update/reveal contract"
      pattern: "put\\(\"/api/v1/config\""
    - from: "backend/app/models/run.py"
      to: "backend/app/models/stage.py"
      via: "foreign key / relationship"
      pattern: "run_id|project_id"
---

<objective>
Lock the existing creator bootstrap flow and introduce canonical lineage tables without disturbing the current manual runtime.

Purpose: preserve the working project/config entry surface while giving later phases a stable project/run/stage/artifact data model.
Output: regression tests plus new SQLModel lineage tables that are registered in the metadata bootstrap path.
</objective>

<execution_context>
@/home/xeron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/xeron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-bootstrap/01-RESEARCH.md
@backend/app/api/v1/routes/projects.py
@backend/app/api/v1/routes/config.py
@backend/app/services/config_service.py
@backend/app/models/project.py
@backend/app/models/agent_run.py
@backend/tests/conftest.py
@frontend/app/components/settings/SettingsModal.tsx
@frontend/app/pages/NewProjectPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the bootstrap contracts</name>
  <files>backend/tests/conftest.py, backend/tests/test_api/test_projects.py, backend/tests/test_api/test_config.py, backend/tests/test_domain/test_lineage_models.py</files>
  <behavior>
    - Add failing tests that assert project creation still persists title/story/style exactly as the current UI sends it.
    - Add failing tests that assert provider config behavior still honors masking, reveal, and DB/env precedence for the text/image/video surface.
    - Add failing model-contract tests that describe the expected Run/Stage/Artifact lineage shape before any implementation exists.
  </behavior>
  <action>Write the regression and contract tests first. Keep them anchored to the current API payloads and config groups; do not introduce a new UI flow or rename the existing project/config endpoints.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_api/test_projects.py tests/test_api/test_config.py tests/test_domain/test_lineage_models.py -x</automated>
  </verify>
  <done>The bootstrap entry flow is locked by tests, and the new lineage contract tests fail for the right reason before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical lineage tables</name>
  <files>backend/app/models/run.py, backend/app/models/stage.py, backend/app/models/artifact.py, backend/app/db/session.py</files>
  <behavior>
    - Add the new SQLModel tables for Run, Stage, and Artifact with explicit foreign-key lineage back to project/run and version/provenance fields that Phase 3+ can extend.
    - Register the new modules in the metadata bootstrap path so tests and runtime table creation see them.
    - Keep AgentRun and the current manual orchestrator untouched; Phase 1 only adds the canonical model boundary and does not migrate execution flow yet.
  </behavior>
  <action>Implement the lineage model layer and metadata registration so the contract tests from Task 1 pass. Preserve the current runtime behavior; do not move orchestration, checkpointing, or queue logic into these tables yet.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_api/test_projects.py tests/test_api/test_config.py tests/test_domain/test_lineage_models.py -x</automated>
  </verify>
  <done>Run, Stage, and Artifact exist as concrete SQLModel tables, metadata bootstrapping sees them, and the bootstrap regression suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| creator UI → FastAPI project/config routes | Untrusted story, style, and provider inputs cross into API handlers |
| FastAPI ORM → PostgreSQL schema | New lineage rows and existing bootstrap tables are persisted here |
| tests/metadata bootstrap → model registry | Table discovery depends on importing the right SQLModel modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | project/config request handling | mitigate | Keep Pydantic validation and the existing config whitelist/masking behavior; do not widen the accepted payload surface in Phase 1. |
| T-01-02 | I | config values in UI/API | mitigate | Preserve the current sensitive-key masking/reveal semantics and assert them in regression tests before changing anything else. |
| T-01-03 | T/R | lineage tables and metadata bootstrap | mitigate | Use explicit foreign keys plus import-based metadata registration so Run/Stage/Artifact cannot drift from the schema the tests expect. |
</threat_model>

<verification>
Run the targeted bootstrap regression suite and confirm the lineage contract tests fail before implementation, then pass after the model layer lands.
</verification>

<success_criteria>
- Project creation still works with the current title/story/style form payload.
- Provider config continues to honor the existing text/image/video grouping, masking, and reveal behavior.
- Run, Stage, and Artifact are real SQLModel tables with lineage back to the project/run graph.
- The backend metadata bootstrap path can discover the new tables without manual test hacks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-bootstrap/01-01-SUMMARY.md`
</output>
