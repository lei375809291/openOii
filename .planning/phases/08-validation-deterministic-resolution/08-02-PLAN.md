---
phase: 08-validation-deterministic-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - backend/app/api/v1/routes/generation.py
  - backend/app/exceptions.py
  - backend/app/schemas/project.py
  - backend/tests/test_api/test_generation.py
  - backend/tests/test_api/test_phase2_generation.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - "generate 前必须先做 provider preflight。"
    - "active/recoverable 冲突仍然优先返回 409。"
    - "provider invalid 返回结构化 422，且 invalid path 不创建 run。"
  artifacts:
    - path: "backend/app/api/v1/routes/generation.py"
      provides: "generate preflight with 409 > 422 priority"
    - path: "backend/tests/test_api/test_generation.py"
      provides: "invalid provider -> 422 / no run creation regression coverage"
    - path: "backend/tests/test_api/test_phase2_generation.py"
      provides: "409 active/recoverable precedence stays intact"
  key_links:
    - from: "backend/app/api/v1/routes/generation.py"
      to: "backend/app/services/provider_resolution.py"
      via: "preflight before AgentRun creation"
      pattern: "resolved_key|valid|reason_code"
---

<objective>
在 generate 前接入 provider preflight，明确 409 与 422 的优先级，并保证 invalid provider 不会创建 run。

Purpose: 把 Phase 08 的 resolver 真相真正变成启动前阻断规则，满足 VAL-01 并为后续 runtime snapshot phase 提供可靠入口语义。
Output: generate preflight、结构化 provider invalid 422、无 run creation 的 invalid 路径测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-validation-deterministic-resolution/08-CONTEXT.md
@.planning/phases/08-validation-deterministic-resolution/08-RESEARCH.md
@.planning/phases/08-validation-deterministic-resolution/08-01-PLAN.md
@backend/app/api/v1/routes/generation.py
@backend/app/exceptions.py
@backend/tests/test_api/test_generation.py
@backend/tests/test_api/test_phase2_generation.py

<interfaces>
Required 422 error envelope (reuse existing app exception handler):
```json
{
  "error": {
    "code": "PROVIDER_PRECHECK_FAILED",
    "message": "项目 Provider 配置无效，无法启动生成",
    "details": {
      "provider_resolution": {
        "valid": false,
        "modalities": {
          "text": {
            "selected_key": "openai",
            "source": "project",
            "resolved_key": null,
            "valid": false,
            "reason_code": "provider_missing_credentials",
            "reason_message": "..."
          }
        }
      }
    }
  }
}
```

Precedence to preserve:
```text
1. active run -> 409
2. recoverable run -> 409
3. provider invalid -> 422
4. only then create AgentRun and spawn orchestrator
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 generate preflight 的 409/422 优先级与 no-run 语义</name>
  <files>backend/tests/test_api/test_generation.py, backend/tests/test_api/test_phase2_generation.py</files>
  <read_first>
    - backend/app/api/v1/routes/generation.py
    - backend/tests/test_api/test_generation.py
    - backend/tests/test_api/test_phase2_generation.py
    - backend/app/main.py
  </read_first>
  <behavior>
    - Test 1: active run 存在时，即便 provider invalid，也仍返回 `409`，不返回 `422`。
    - Test 2: recoverable run 存在时，即便 provider invalid，也仍返回 `409`。
    - Test 3: 无 active/recoverable 且 provider invalid 时，`POST /generate` 返回 `422`，`error.code='PROVIDER_PRECHECK_FAILED'`。
    - Test 4: provider invalid 路径不会插入新的 `AgentRun` 记录。
  </behavior>
  <action>扩展现有 generate API 测试：在 test 中 monkeypatch 08-01 引入的 resolver/preflight 结果，分别构造 active、recoverable、invalid 三种情形。断言 invalid 422 响应必须带 `provider_resolution` 明细，并在 invalid 路径前后比较 `AgentRun` 数量确保未创建 run。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_phase2_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_api/test_generation.py` contains `PROVIDER_PRECHECK_FAILED` assertion.
    - `backend/tests/test_api/test_generation.py` contains an assertion that run count or inserted run id does not change on invalid path.
    - `backend/tests/test_api/test_phase2_generation.py` still asserts `409` for active and recoverable run cases.
    - Running `uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_phase2_generation.py -q` fails before implementation for the new 422/no-run expectations.
  </acceptance_criteria>
  <done>测试先失败，并精确锁定“409 优先于 422、invalid 不创建 run”的生成入口合同。</done>
</task>

<task type="auto">
  <name>Task 2: 在 generate 路由中接入 preflight，并用结构化 422 阻断 invalid provider</name>
  <files>backend/app/api/v1/routes/generation.py, backend/app/exceptions.py, backend/app/schemas/project.py, backend/tests/test_api/test_generation.py, backend/tests/test_api/test_phase2_generation.py</files>
  <read_first>
    - backend/app/api/v1/routes/generation.py
    - backend/app/exceptions.py
    - backend/app/schemas/project.py
    - backend/tests/test_api/test_generation.py
    - backend/tests/test_api/test_phase2_generation.py
    - backend/app/services/provider_resolution.py
  </read_first>
  <action>保持 generation 路由现有 409 分支顺序不变：先 active，再 recoverable。只有在两条 409 路径都未命中后，调用 08-01 的 resolver 对当前 project + runtime settings 做 preflight。若任一 modality `valid=False`，抛出结构化 `BusinessError(code='PROVIDER_PRECHECK_FAILED')`，message 固定为“项目 Provider 配置无效，无法启动生成”，`details.provider_resolution` 填入 resolver 全量结果；此时严禁创建 `AgentRun`。只有 preflight 全绿时，才执行当前的 `AgentRun(status='running')` 创建与 `asyncio.create_task()` 启动逻辑。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_phase2_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/v1/routes/generation.py` contains provider preflight before `run = AgentRun(`.
    - `backend/app/api/v1/routes/generation.py` contains `PROVIDER_PRECHECK_FAILED` and `provider_resolution` in the error details path.
    - `backend/app/api/v1/routes/generation.py` still contains `status.HTTP_409_CONFLICT` branches above the preflight block.
    - `uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_phase2_generation.py -q` exits 0.
  </acceptance_criteria>
  <done>generate 入口已能在真正创建 run 前阻断 invalid provider，并保持原有 recovery 冲突优先级。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project start request → generation route | creator 点击生成，进入 run creation 边界 |
| generation route → database | 是否创建 AgentRun 是高价值状态转换 |
| generation route → frontend error surface | 结构化 422 将直接驱动 creator-facing 阻断理由 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | T | `backend/app/api/v1/routes/generation.py` | mitigate | preflight 必须发生在 `AgentRun` 创建前，invalid 时直接 422。 |
| T-08-06 | D | generation conflict handling | mitigate | 保持 active/recoverable `409` 优先级，防止 preflight 覆盖已有恢复语义。 |
| T-08-07 | I | 422 details | mitigate | 只返回 `provider_resolution`、reason code/message，不返回 API key 或其它 secret。 |
</threat_model>

<verification>
- `uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_phase2_generation.py -q`
</verification>

<success_criteria>
- active/recoverable run 仍先返回 409。
- provider invalid 时 generate 返回结构化 422，且包含稳定 reason 信息。
- invalid 路径不会创建 AgentRun 或启动后台任务。
</success_criteria>

<output>
After completion, create `.planning/phases/08-validation-deterministic-resolution/08-02-SUMMARY.md`
</output>
