---
phase: 08-validation-deterministic-resolution
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/app/types/index.ts
  - frontend/app/pages/ProjectPage.tsx
  - frontend/app/pages/ProjectPage.test.tsx
  - frontend/app/components/chat/ChatPanel.tsx
  - frontend/app/services/api.ts
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - "项目页必须展示真实解析结果，而不是仅展示保存值。"
    - "provider invalid 时创作者在点击生成前就会被阻断。"
    - "前端阻断使用后端返回的 reason，而不是前端自行猜测。"
  artifacts:
    - path: "frontend/app/pages/ProjectPage.tsx"
      provides: "creator-facing provider resolution proof and generate blocking"
    - path: "frontend/app/components/chat/ChatPanel.tsx"
      provides: "disabled generate CTA with blocking reason"
    - path: "frontend/app/types/index.ts"
      provides: "frontend types aligned to Phase 08 resolver contract"
  key_links:
    - from: "frontend/app/pages/ProjectPage.tsx"
      to: "backend/app/api/v1/routes/projects.py"
      via: "project.provider_settings resolution rendering"
      pattern: "selected_key|resolved_key|valid|reason_code|reason_message"
    - from: "frontend/app/pages/ProjectPage.tsx"
      to: "frontend/app/components/chat/ChatPanel.tsx"
      via: "generate disabled reason"
      pattern: "disabled|reason"
---

<objective>
做前端最小阻断面：项目页显示真实解析结果，provider invalid 时禁用生成，并在 stale/direct-call 情况下保持 422 提示一致。

Purpose: 让 creator 在运行前就能看到“会用什么 provider / 为什么不能启动”，完成 Phase 08 的 creator-facing proof，而不扩展到 runtime evidence 或 lineage UI。
Output: 对齐后的前端类型、项目页 resolution proof card、禁用生成按钮与统一错误提示。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-validation-deterministic-resolution/08-CONTEXT.md
@.planning/phases/08-validation-deterministic-resolution/08-RESEARCH.md
@.planning/phases/08-validation-deterministic-resolution/08-01-PLAN.md
@.planning/phases/08-validation-deterministic-resolution/08-02-PLAN.md
@frontend/app/types/index.ts
@frontend/app/pages/ProjectPage.tsx
@frontend/app/pages/ProjectPage.test.tsx
@frontend/app/components/chat/ChatPanel.tsx
@frontend/app/services/api.ts

<interfaces>
Phase 08 frontend provider entry target:
```typescript
export interface ProjectProviderEntry {
  selected_key: string;
  source: "project" | "default";
  resolved_key: string | null;
  valid: boolean;
  reason_code: string | null;
  reason_message: string | null;
}
```

Generate blocking rules:
```text
- if any modality.valid === false -> disable generate CTA
- blocking message comes from provider_settings[*].reason_message
- if user still triggers POST /generate through stale UI and gets 422, show the backend error message/details
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定项目页 invalid proof 与禁用生成回归</name>
  <files>frontend/app/pages/ProjectPage.test.tsx</files>
  <read_first>
    - frontend/app/pages/ProjectPage.tsx
    - frontend/app/pages/ProjectPage.test.tsx
    - frontend/app/components/chat/ChatPanel.tsx
    - frontend/app/services/api.ts
    - frontend/app/types/index.ts
  </read_first>
  <behavior>
    - Test 1: `project.provider_settings.video.valid=false`、`resolved_key=null`、`reason_message='缺少 Doubao API Key'` 时，项目页展示 invalid reason。
    - Test 2: 上述 invalid 场景下，“开始生成”按钮不可点。
    - Test 3: 按钮禁用状态来自后端返回数据，而不是前端本地推导 provider 名称。
  </behavior>
  <action>扩展 `ProjectPage.test.tsx`：在现有 proof card 用例基础上构造一个 invalid video provider fixture，断言页面显示 `resolved_key` 缺失对应的提示文案，并且 ChatPanel 的生成按钮处于 disabled。不要把测试写成“只要 source=project 就禁用”；要直接断言 `valid=false` / `reason_message` 驱动 UI。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/pages/ProjectPage.test.tsx` contains assertions for `valid: false`, `resolved_key: null`, and a disabled generate button.
    - Running `pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx` fails before implementation for the new invalid-state expectations.
  </acceptance_criteria>
  <done>测试先失败，并锁定“显示真实 invalid reason + 禁用开始生成”的 creator-facing 阻断行为。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐前端类型与项目页 proof card，展示真实 resolved state</name>
  <files>frontend/app/types/index.ts, frontend/app/pages/ProjectPage.tsx, frontend/app/pages/ProjectPage.test.tsx</files>
  <read_first>
    - frontend/app/types/index.ts
    - frontend/app/pages/ProjectPage.tsx
    - frontend/app/pages/ProjectPage.test.tsx
    - backend/app/schemas/project.py
  </read_first>
  <action>把前端 `ProjectProviderEntry` 类型从旧的 `override_key/effective_key/source` 升级到 Phase 08 的 `selected_key/source/resolved_key/valid/reason_code/reason_message`。更新 `ProjectPage.tsx` 的 provider proof card：每个 modality 至少展示 `selected_key`、`resolved_key`（invalid 时显示 `未解析` 或等价文案）、`source` badge 与 invalid reason。不要在前端重新推导 resolved provider；页面只能消费后端返回字段。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/types/index.ts` defines `resolved_key`, `valid`, `reason_code`, and `reason_message` on `ProjectProviderEntry`.
    - `frontend/app/pages/ProjectPage.tsx` renders `row.entry.resolved_key` (or explicit invalid fallback text when null) and `row.entry.reason_message`.
    - `pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx && pnpm --dir frontend exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>项目页 proof surface 已从“保存值展示”升级为“真实解析结果展示”。</done>
</task>

<task type="auto">
  <name>Task 3: 把 generate CTA 接到 invalid 阻断，并保持 422 提示一致</name>
  <files>frontend/app/components/chat/ChatPanel.tsx, frontend/app/pages/ProjectPage.tsx, frontend/app/services/api.ts, frontend/app/pages/ProjectPage.test.tsx</files>
  <read_first>
    - frontend/app/components/chat/ChatPanel.tsx
    - frontend/app/pages/ProjectPage.tsx
    - frontend/app/services/api.ts
    - frontend/app/pages/ProjectPage.test.tsx
  </read_first>
  <action>扩展 `ChatPanel` props，新增明确的 `generateDisabled?: boolean` 与 `generateDisabledReason?: string`（命名可微调但语义必须一致）。`ProjectPage.tsx` 根据 `project.provider_settings` 聚合出阻断状态：任一 modality `valid=false` 即传入 disabled 与首个 `reason_message`。在 ChatPanel 空状态下禁用“开始生成”按钮，并在按钮附近显示阻断说明。保留 `generateMutation.onError` 对 `ApiError` 的处理；若后端仍返回 `422 PROVIDER_PRECHECK_FAILED`（例如页面数据过期），toast 文案使用后端 message/details，不新增前端猜测逻辑。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/components/chat/ChatPanel.tsx` contains a disabled generate button path.
    - `frontend/app/components/chat/ChatPanel.tsx` renders a blocking reason string when generation is disabled.
    - `frontend/app/pages/ProjectPage.tsx` computes generate blocking from `project.provider_settings.*.valid` rather than local provider-name constants.
    - `pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx && pnpm --dir frontend exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>creator 在点击生成前就能看到 invalid 阻断，且 stale/direct-call 情况下仍与后端 422 保持一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend project read → frontend proof card | creator 依赖该展示判断是否可以启动 |
| frontend proof state → generate CTA | invalid 状态必须真正阻止交互 |
| backend 422 → toast surface | stale UI 时仍需保持阻断理由一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | S | `frontend/app/pages/ProjectPage.tsx` | mitigate | proof card 只消费后端的 `resolved_key/valid/reason_message`，不在前端自行声称 provider 可用。 |
| T-08-09 | T | `frontend/app/components/chat/ChatPanel.tsx` | mitigate | 生成按钮 disabled 必须绑定 project invalid 状态，不能只靠 toast 兜底。 |
| T-08-10 | I | frontend error display | mitigate | 422 只展示后端 message/details，不暴露 secrets；阻断理由来源于后端 reason_message。 |
</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx`
- `pnpm --dir frontend exec tsc --noEmit`
</verification>

<success_criteria>
- 项目页展示真实解析结果，并能在 invalid 时说明原因。
- invalid provider 时，creator 在点击生成前就已被禁用 CTA 阻断。
- stale 页面即便仍调用到 generate，也会把后端 422 作为同一阻断语义呈现出来。
</success_criteria>

<output>
After completion, create `.planning/phases/08-validation-deterministic-resolution/08-03-SUMMARY.md`
</output>
