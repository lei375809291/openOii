---
phase: 08-validation-deterministic-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/provider_resolution.py
  - backend/app/schemas/project.py
  - backend/app/api/v1/routes/projects.py
  - backend/app/services/text_factory.py
  - backend/app/services/video_factory.py
  - backend/tests/test_services/test_provider_resolution.py
  - backend/tests/test_api/test_projects.py
  - frontend/app/components/project/ProviderSelectionFields.tsx
  - frontend/app/components/settings/SettingsModal.tsx
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - "provider 默认值必须来自 runtime settings / live config，而不是 schema 或前端常量。"
    - "resolver 对同样输入永远返回同样输出；解析失败时 resolved_key 必须为 null。"
    - "text/video factory 不再对未知 provider 静默 fallback 到别的服务。"
  artifacts:
    - path: "backend/app/services/provider_resolution.py"
      provides: "统一 provider resolution truth contract"
    - path: "backend/app/api/v1/routes/projects.py"
      provides: "项目页读取的真实 provider resolution/proof surface"
    - path: "backend/app/services/text_factory.py"
      provides: "文本 provider 显式解析失败而非 silent fallback"
    - path: "backend/app/services/video_factory.py"
      provides: "视频 provider 显式解析失败而非 silent fallback"
  key_links:
    - from: "backend/app/services/provider_resolution.py"
      to: "backend/app/api/v1/routes/projects.py"
      via: "project provider_settings serialization"
      pattern: "selected_key|resolved_key|valid|reason_code|reason_message"
    - from: "backend/app/services/provider_resolution.py"
      to: "backend/app/services/text_factory.py"
      via: "resolved provider input only"
      pattern: "anthropic|openai"
    - from: "backend/app/services/provider_resolution.py"
      to: "backend/app/services/video_factory.py"
      via: "resolved provider input only"
      pattern: "openai|doubao"
---

<objective>
先收敛 provider 解析真相：建立唯一 resolver，统一 project/default 语义，并去掉 text/video factory 的 silent fallback。

Purpose: Phase 08 后续的 generate preflight 与前端阻断面都必须建立在同一份解析合同上，否则 UI proof 与真实运行会继续分叉。
Output: resolver 服务、扩展后的 project provider contract、显式失败的 text/video factory、去硬编码默认值的前端默认文案来源。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-validation-deterministic-resolution/08-CONTEXT.md
@.planning/phases/08-validation-deterministic-resolution/08-RESEARCH.md
@backend/app/config.py
@backend/app/schemas/project.py
@backend/app/api/v1/routes/projects.py
@backend/app/services/text_factory.py
@backend/app/services/video_factory.py
@frontend/app/components/project/ProviderSelectionFields.tsx
@frontend/app/components/settings/SettingsModal.tsx

<interfaces>
Current backend project contract:
```python
class ProjectProviderEntry(BaseModel):
    override_key: str | None
    effective_key: str
    source: Literal["project", "default"]
```

Target Phase 08 contract for each modality:
```python
class ProviderResolutionEntry(BaseModel):
    selected_key: str
    source: Literal["project", "default"]
    resolved_key: str | None
    valid: bool
    reason_code: str | None
    reason_message: str | None
```

Capability matrix to hard-code in this phase:
```python
TEXT = ("anthropic", "openai")
IMAGE = ("openai",)
VIDEO = ("openai", "doubao")
```

Invalid contract rules to enforce:
```python
resolved_key = None  # whenever valid == False
no silent fallback   # unknown/unsupported/misconfigured provider must not switch provider
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 resolver 与 project proof contract 回归</name>
  <files>backend/tests/test_services/test_provider_resolution.py, backend/tests/test_api/test_projects.py</files>
  <read_first>
    - backend/app/config.py
    - backend/app/schemas/project.py
    - backend/app/api/v1/routes/projects.py
    - backend/tests/test_api/test_projects.py
  </read_first>
  <behavior>
    - Test 1: resolver 在 `settings.text_provider='openai'`、项目未设 override 时，text 返回 `selected_key='openai'`、`source='default'`、`resolved_key='openai'`、`valid=True`。
    - Test 2: resolver 在 `video_provider_override='doubao'` 但 `doubao_api_key=None` 时，video 返回 `selected_key='doubao'`、`resolved_key=None`、`valid=False`、`reason_code='provider_missing_credentials'`。
    - Test 3: 同样输入调用 resolver 两次，输出完全一致。
    - Test 4: `GET /api/v1/projects/:id` 返回的 provider proof 不再只给 `effective_key`，而是返回 `selected_key/source/resolved_key/valid/reason_code/reason_message`。
  </behavior>
  <action>新增一个专门的 resolver 测试文件，覆盖 default sourcing、missing credentials、determinism 三类核心行为；同时扩展 `test_projects.py`，把项目读模型断言从旧的 `effective_key/source` 升级到新的完整解析合同。测试里明确用 runtime settings 改变默认值（例如 text=openai、video=doubao）证明 project proof surface 来自 settings 而不是 `DEFAULT_*_PROVIDER` 常量。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_services/test_provider_resolution.py` exists and contains `selected_key`, `resolved_key`, `reason_code`, `same input`-style determinism assertions.
    - `backend/tests/test_api/test_projects.py` contains assertions for `provider_settings.text.selected_key` and `provider_settings.video.valid`.
    - Running `uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q` fails before implementation for the new expectations.
  </acceptance_criteria>
  <done>测试先失败，并准确锁定“默认来自 runtime settings、invalid 无 resolved_key、project proof 复用 resolver”的 Phase 08 真相合同。</done>
</task>

<task type="auto">
  <name>Task 2: 实现统一 resolver，并让 projects route 改读真实解析结果</name>
  <files>backend/app/services/provider_resolution.py, backend/app/schemas/project.py, backend/app/api/v1/routes/projects.py, backend/tests/test_services/test_provider_resolution.py, backend/tests/test_api/test_projects.py</files>
  <read_first>
    - backend/app/config.py
    - backend/app/schemas/project.py
    - backend/app/api/v1/routes/projects.py
    - backend/app/models/project.py
    - backend/tests/test_services/test_provider_resolution.py
    - backend/tests/test_api/test_projects.py
  </read_first>
  <action>新增 `backend/app/services/provider_resolution.py`，提供一个统一入口（函数名可自定，但必须只接受 project overrides + runtime settings，并返回 text/image/video 三个 modality 的解析结果）。resolver 里硬编码 capability matrix：text=`anthropic|openai`，image=`openai`，video=`openai|doubao`，merge 不参与。对 credentials 的最小有效性检查使用现有 settings 字段：Anthropic 需要 `anthropic_api_key` 或 `anthropic_auth_token`；text-openai 需要 `text_api_key`；image-openai 需要 `image_api_key`；video-openai 需要 `video_api_key`；video-doubao 需要 `doubao_api_key`。扩展 `backend/app/schemas/project.py` 的 read DTO，让 `provider_settings` entry 变成 `selected_key/source/resolved_key/valid/reason_code/reason_message`；`backend/app/api/v1/routes/projects.py` 停止直接使用 `DEFAULT_*_PROVIDER` 拼装 proof，而是调用 resolver 序列化真实结果。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/provider_resolution.py` contains capability matrix entries for `anthropic`, `openai`, and `doubao`.
    - `backend/app/services/provider_resolution.py` contains `resolved_key = None` (or equivalent explicit null assignment) on invalid paths.
    - `backend/app/api/v1/routes/projects.py` no longer imports or uses `DEFAULT_TEXT_PROVIDER`, `DEFAULT_IMAGE_PROVIDER`, or `DEFAULT_VIDEO_PROVIDER` to build response defaults.
    - `backend/app/schemas/project.py` defines project read entries with `selected_key`, `resolved_key`, `valid`, `reason_code`, and `reason_message`.
    - `uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q` exits 0.
  </acceptance_criteria>
  <done>resolver 成为唯一 truth source，项目页读取到的 provider proof 已经反映真实 runtime 默认与 validity。</done>
</task>

<task type="auto">
  <name>Task 3: 去掉 text/video factory silent fallback，并统一前端默认文案来源</name>
  <files>backend/app/services/text_factory.py, backend/app/services/video_factory.py, frontend/app/components/project/ProviderSelectionFields.tsx, frontend/app/components/settings/SettingsModal.tsx</files>
  <read_first>
    - backend/app/services/text_factory.py
    - backend/app/services/video_factory.py
    - backend/app/config.py
    - frontend/app/components/project/ProviderSelectionFields.tsx
    - frontend/app/components/settings/SettingsModal.tsx
  </read_first>
  <action>把 `backend/app/services/text_factory.py` 与 `backend/app/services/video_factory.py` 的 `else -> 默认 provider` 路径改成显式失败：未知 provider key 必须抛错（例如 `ValueError("Unsupported ... provider")`），禁止静默落到 Anthropic/OpenAI。同步修正前端默认语义来源：`ProviderSelectionFields.tsx` 不再把“继承默认（当前：X）”写死为常量，而要接收来自父层的当前默认解析值；`SettingsModal.tsx` 的 `getVideoProvider()` 缺省回退从 `'doubao'` 改成与 `backend/app/config.py:video_provider` 一致的 `'openai'`，消除已知默认值不一致点。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/text_factory.py` contains no `else:` branch that returns `LLMService(settings)` for unknown providers.
    - `backend/app/services/video_factory.py` contains no `else:` branch that returns `VideoService(settings)` for unknown providers.
    - `frontend/app/components/settings/SettingsModal.tsx` contains `openai` as the video provider fallback and no longer contains `|| 'doubao'` in `getVideoProvider()`.
    - `pnpm --dir frontend exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>text/video provider 选择不再 silently fallback；前后端默认值语义至少在 video 已知分叉点上收敛。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project overrides + runtime settings → resolver | 用户输入与系统配置共同决定运行前 proof surface |
| resolver → factory selection | 真实执行 provider 选择从解析合同进入服务工厂 |
| resolver → frontend default labels | creator-visible 默认 provider 文案依赖后端真实默认 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `backend/app/api/v1/routes/projects.py` | mitigate | 停止用 schema 常量拼 proof，统一改读 resolver 输出。 |
| T-08-02 | T | `backend/app/services/provider_resolution.py` | mitigate | invalid 时强制 `resolved_key = null`，防止伪造可用 provider。 |
| T-08-03 | S | `backend/app/services/text_factory.py` / `video_factory.py` | mitigate | 未知 provider key 直接抛错，禁止 silent fallback 冒充合法执行。 |
| T-08-04 | I | `frontend/app/components/project/ProviderSelectionFields.tsx` | mitigate | “继承默认”文案从实时解析值传入，不从常量猜测当前默认。 |
</threat_model>

<verification>
- `uv run --project backend pytest backend/tests/test_services/test_provider_resolution.py backend/tests/test_api/test_projects.py -q`
- `pnpm --dir frontend exec tsc --noEmit`
</verification>

<success_criteria>
- provider 解析由单一 resolver 输出，默认值来自 runtime settings。
- 无论 text 还是 video，未知或不支持的 provider key 都不会再 silent fallback。
- 项目页读取到的 provider proof 已经是 Phase 08 的真实解析结果，而不是 Phase 07 的静态近似值。
</success_criteria>

<output>
After completion, create `.planning/phases/08-validation-deterministic-resolution/08-01-SUMMARY.md`
</output>
