---
phase: 07-project-provider-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/app/types/index.ts
  - frontend/app/services/api.ts
  - frontend/app/components/project/ProviderSelectionFields.tsx
  - frontend/app/pages/NewProjectPage.tsx
  - frontend/app/pages/NewProjectPage.test.tsx
  - frontend/app/pages/ProjectPage.tsx
  - frontend/app/pages/ProjectPage.test.tsx
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
must_haves:
  truths:
    - "创作者可以在项目创建/编辑界面分别选择 text、image、video provider。"
    - "刷新项目页后仍能看到与后端一致的 provider 选择。"
    - "项目页会明确展示每个 modality 是项目覆盖还是默认继承。"
  artifacts:
    - path: "frontend/app/components/project/ProviderSelectionFields.tsx"
      provides: "共享的 provider 选择字段"
    - path: "frontend/app/pages/NewProjectPage.tsx"
      provides: "项目创建时的 provider 选择提交"
    - path: "frontend/app/pages/ProjectPage.tsx"
      provides: "项目页 provider proof/edit card"
    - path: "frontend/app/types/index.ts"
      provides: "Project provider contract TS types"
  key_links:
    - from: "frontend/app/services/api.ts"
      to: "backend/app/api/v1/routes/projects.py"
      via: "projectsApi.create/update/get"
      pattern: "provider_settings|text_provider_override|image_provider_override|video_provider_override"
    - from: "frontend/app/pages/ProjectPage.tsx"
      to: "frontend/app/types/index.ts"
      via: "Project.provider_settings rendering"
      pattern: "source|effective_key"
---

<objective>
把 Phase 07 后端合同落到 creator-facing UI：能选、能看、能分辨来源。

Purpose: 创作者必须直接在项目流里看到 provider persistence 生效，否则 portability 仍然只是后端内部能力。
Output: 新建项目 provider 选择、项目页 provider summary/edit card、前端合同与回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/app/pages/NewProjectPage.tsx
@frontend/app/pages/ProjectPage.tsx
@frontend/app/services/api.ts
@frontend/app/types/index.ts

<interfaces>
From plan 07-01 backend contract:
```typescript
export interface ProjectProviderEntry {
  override_key: string | null;
  effective_key: string;
  source: "project" | "default";
}

export interface ProjectProviderSettings {
  text: ProjectProviderEntry;
  image: ProjectProviderEntry;
  video: ProjectProviderEntry;
}

export interface Project {
  id: number;
  title: string;
  story: string | null;
  style: string | null;
  summary: string | null;
  video_url: string | null;
  status: string;
  created_at: string;
  updated_at: string;
  provider_settings: ProjectProviderSettings;
  text_provider_override: string | null;
  image_provider_override: string | null;
  video_provider_override: string | null;
}
```

Use these provider options only in Phase 07:
```typescript
const TEXT_PROVIDER_OPTIONS = ["inherit-default", "anthropic", "openai"];
const IMAGE_PROVIDER_OPTIONS = ["inherit-default", "openai"];
const VIDEO_PROVIDER_OPTIONS = ["inherit-default", "openai", "doubao"];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定前端 provider 提交与展示回归测试</name>
  <files>frontend/app/pages/NewProjectPage.test.tsx, frontend/app/pages/ProjectPage.test.tsx, frontend/app/types/index.ts, frontend/app/services/api.ts</files>
  <behavior>
    - Test 1: NewProjectPage 提交 payload 时包含三类 override 字段，并把“继承默认”提交为 `null`。
    - Test 2: ProjectPage 根据 `project.provider_settings` 渲染 effective provider 与 source badge。
    - Test 3: ProjectPage 保存 provider 更新后只刷新项目数据，不清空现有 realtime/recovery state。
  </behavior>
  <action>先扩展现有页面测试，锁定 Phase 07 的 creator-visible contract。把新的 TS 类型与 API payload shape 一起补上，确保后续实现不会在 `projectsApi.create/update/get` 与页面之间漂移。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/NewProjectPage.test.tsx app/pages/ProjectPage.test.tsx</automated>
  </verify>
  <done>页面测试先失败并明确描述 provider 选择提交、回显与来源展示的预期。</done>
</task>

<task type="auto">
  <name>Task 2: 在项目创建流里加入 provider 选择字段并提交 override</name>
  <files>frontend/app/components/project/ProviderSelectionFields.tsx, frontend/app/pages/NewProjectPage.tsx, frontend/app/services/api.ts, frontend/app/types/index.ts</files>
  <action>创建共享 `ProviderSelectionFields` 组件，封装 text/image/video 三组选择控件与“继承默认”选项。把它接入 `NewProjectPage` 的 step flow：默认全部继承（提交为 `null`），确认页展示当前三类选择，创建 payload 显式传 `text_provider_override`、`image_provider_override`、`video_provider_override`。不要借用全局 `SettingsModal`；那是系统默认配置入口，不是项目级 portability 入口。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/NewProjectPage.test.tsx</automated>
  </verify>
  <done>新建项目时可以分别选择三类 provider，点击创建后 payload 与 UI 选择一致，并保留“继承默认”的语义。</done>
</task>

<task type="auto">
  <name>Task 3: 在项目页展示并编辑 provider proof card</name>
  <files>frontend/app/components/project/ProviderSelectionFields.tsx, frontend/app/pages/ProjectPage.tsx, frontend/app/pages/ProjectPage.test.tsx, frontend/app/services/api.ts, frontend/app/types/index.ts</files>
  <action>在 `ProjectPage` header/recovery banner 之间加入 provider card，逐项展示 text/image/video 的 `effective_key` 与来源 badge（`项目覆盖` / `默认继承`）。提供内联编辑/保存动作，通过 `projectsApi.update` 持久化 override，并在成功后只 invalidates `['project', projectId]` / `['projects']`，不要清空 store 里的 live progress、recovery control、messages。刷新页面后继续从 `projectsApi.get` 回显相同 provider state。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/NewProjectPage.test.tsx app/pages/ProjectPage.test.tsx && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>项目页能清楚展示并编辑三类 provider，刷新后仍正确回显，并且不会破坏现有恢复/实时进度状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form→projects API | 用户可篡改 provider 选择表单输入 |
| project query data→UI rendering | 后端 provider source/effective 数据进入 creator-visible proof surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T | `ProviderSelectionFields.tsx` | mitigate | 仅渲染受控选项并把“继承默认”映射为 `null`，不允许自由文本输入 provider key。 |
| T-07-06 | I | `ProjectPage.tsx` | mitigate | 只展示 provider key/source，不缓存或展示任何 secret/config value。 |
| T-07-07 | D | `ProjectPage.tsx` store integration | mitigate | 保存后只刷新 React Query project caches，禁止 reset editor store，避免 UI 自己造成运行态丢失。 |
| T-07-08 | S | UI source badge | mitigate | source badge 完全以后端 `provider_settings.*.source` 为准，不在前端自行推导真假来源。 |
</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run app/pages/NewProjectPage.test.tsx app/pages/ProjectPage.test.tsx`
- `pnpm --dir frontend exec tsc --noEmit`
- 手动 spot-check：NewProjectPage 提交 payload 与 ProjectPage 回显字段一致。
</verification>

<success_criteria>
- 创作者能在新建或编辑项目时分别设置 text/image/video provider。
- 项目页刷新后仍显示与后端相同的 provider 选择。
- 项目页对每个 modality 明确展示项目覆盖还是默认继承。
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-provider-contracts/07-02-SUMMARY.md`
</output>
