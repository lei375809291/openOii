---
phase: 07-project-provider-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - frontend/app/pages/ProjectPage.tsx
  - frontend/app/pages/ProjectPage.test.tsx
  - frontend/app/types/index.ts
  - frontend/app/utils/workspaceStatus.test.ts
autonomous: true
gap_closure: true
requirements:
  - PROV-02
must_haves:
  truths:
    - "创作者刷新项目页后再点“编辑 Provider”，三组单选仍与刚保存的 provider 选择一致。"
    - "ProjectPage 编辑态预填只依赖真实 ProjectRead 合同，不依赖接口未返回的 raw override 字段。"
  artifacts:
    - path: "frontend/app/pages/ProjectPage.tsx"
      provides: "基于 provider_settings.override_key 的编辑态 draft hydration"
    - path: "frontend/app/pages/ProjectPage.test.tsx"
      provides: "真实 GET 合同下的刷新后编辑态回归测试"
    - path: "frontend/app/types/index.ts"
      provides: "与后端 ProjectRead 对齐的前端 Project 类型"
  key_links:
    - from: "backend/app/api/v1/routes/projects.py"
      to: "frontend/app/pages/ProjectPage.tsx"
      via: "projectsApi.get(projectId) -> provider_settings.*.override_key"
      pattern: "provider_settings"
    - from: "frontend/app/pages/ProjectPage.tsx"
      to: "frontend/app/components/project/ProviderSelectionFields.tsx"
      via: "derived providerDraft value"
      pattern: "text_provider_override|image_provider_override|video_provider_override"
---

<objective>
修复 Phase 07 的唯一闭环缺口：项目页刷新后，编辑态仍要准确回显已保存的 provider 选择。

Purpose: 让 PROV-02 在 creator-facing edit flow 上真正成立，并消除前端对不存在接口字段的合同漂移。
Output: 刷新后编辑态回显修复、真实 GET 合同测试、对齐后的前端 Project 类型。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-project-provider-contracts/07-VERIFICATION.md
@.planning/phases/07-project-provider-contracts/07-02-SUMMARY.md
@.planning/milestones/v1.0-phases/02-langgraph-orchestration-durable-execution/02-04-SUMMARY.md
@frontend/app/pages/ProjectPage.tsx
@frontend/app/pages/ProjectPage.test.tsx
@frontend/app/types/index.ts
@frontend/app/components/project/ProviderSelectionFields.tsx

<interfaces>
From backend/app/schemas/project.py (actual read contract):
```python
class ProjectProviderEntry(BaseModel):
    override_key: str | None
    effective_key: str
    source: Literal["project", "default"]

class ProjectProviderSettingsRead(BaseModel):
    text: ProjectProviderEntry
    image: ProjectProviderEntry
    video: ProjectProviderEntry

class ProjectRead(BaseModel):
    id: int
    title: str
    story: str | None
    style: str | None
    summary: str | None
    video_url: str | None
    status: str
    provider_settings: ProjectProviderSettingsRead
    created_at: datetime
    updated_at: datetime
```

From frontend/app/types/index.ts (payload contract to preserve):
```typescript
export interface ProjectProviderOverridesPayload {
  text_provider_override: string | null;
  image_provider_override: string | null;
  video_provider_override: string | null;
}
```

Carry forward from 07-02 / 02-04:
- 项目页保存 provider 后仍然只能 invalidate `['project', projectId]` / `['projects']`，不能 reset live progress 或 recovery state。
- `ProviderSelectionFields` 继续提交 raw override/null；本次只修正 edit draft 的来源，不改保存 payload 形状。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁定真实 ProjectRead 合同下的刷新后编辑态回归</name>
  <files>frontend/app/pages/ProjectPage.test.tsx, frontend/app/utils/workspaceStatus.test.ts</files>
  <behavior>
    - Test 1: `projectsApi.get` 返回只有 `provider_settings`、没有三个 raw override 字段时，ProjectPage 只读 proof card 仍显示正确 effective/source。
    - Test 2: 在上述真实响应下点击“编辑 Provider”后，文本/video 仍默认选中已保存 override，image 仍选中“继承默认（当前：OpenAI）”。
    - Test 3: 保存时 mutation payload 继续发送 `{text,image,video}_provider_override`，且不重置 live progress / recovery 相关 store 状态。
  </behavior>
  <action>把 `ProjectPage.test.tsx` 的 mock project 改成真实后端 `ProjectRead` 形状，删除人工补上的 `text_provider_override` / `image_provider_override` / `video_provider_override` 字段，直接暴露当前合同缺口。补一条显式断言：刷新后进入编辑态，三个 fieldset 的默认选中值必须与 proof card 对应的 `provider_settings.*.override_key` 保持一致。同步清理依赖旧 `Project` 读模型的前端 fixture，让测试反映真实接口而不是 mock-only 字段。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx app/utils/workspaceStatus.test.ts</automated>
  </verify>
  <done>测试先失败，并准确说明“真实 GET 合同缺少 raw override 字段时，编辑态仍必须正确回显保存值”的预期。</done>
</task>

<task type="auto">
  <name>Task 2: 用 provider_settings 派生编辑 draft 并对齐前端读模型</name>
  <files>frontend/app/pages/ProjectPage.tsx, frontend/app/types/index.ts, frontend/app/pages/ProjectPage.test.tsx, frontend/app/utils/workspaceStatus.test.ts</files>
  <action>保持 07-01 已锁定的后端读合同不变，选择 verification 建议的最小修复路径：在 `ProjectPage` 内新增明确 helper（或等价内联逻辑），从 `project.provider_settings.text/image/video.override_key` 派生 `ProjectProviderOverridesPayload`，并在初始 hydration 与“取消编辑”两个入口复用这条逻辑。`Project` 前端类型改为匹配真实 `ProjectRead`：保留 `provider_settings`，移除读模型上的三个 raw override 字段；但继续保留 create/update payload 的 override/null 字段，因为保存 API 仍要求它们。不要改动 provider proof card、mutation invalidation 范围或 Phase 02 的 recovery/live-progress store 语义。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx app/utils/workspaceStatus.test.ts && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>ProjectPage 刷新后进入编辑态时三组单选与后端返回的 `provider_settings` 一致；保存 payload 仍正确发送 raw override/null；前端 `Project` 读模型与真实后端合同不再漂移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend ProjectRead → frontend Project type | 服务端 provider 合同进入前端状态与表单预填 |
| project query data → edit form state | creator-visible proof surface 被转换为可提交的 override payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | T | `frontend/app/pages/ProjectPage.tsx` | mitigate | 编辑态 draft 仅从后端返回的 `provider_settings.*.override_key` 派生，禁止依赖 mock-only 字段导致值被篡改成“继承默认”。 |
| T-07-10 | S | `frontend/app/pages/ProjectPage.test.tsx` | mitigate | 用真实 `ProjectRead` 形状写回归测试，避免测试伪造不存在字段，防止前端误以为合同已满足。 |
| T-07-11 | D | `frontend/app/pages/ProjectPage.tsx` | mitigate | 继续只 invalidate React Query 的 project caches，不 reset recovery/live-progress store，避免修 provider 表单时破坏运行态。 |
</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx app/utils/workspaceStatus.test.ts`
- `pnpm --dir frontend exec tsc --noEmit`
- Spot-check: 刷新一个已保存 text=openai、video=doubao 的项目后，点击“编辑 Provider”，文本/video 单选仍默认选中对应 provider，图像仍为继承默认。
</verification>

<success_criteria>
- 真实 `GET /api/v1/projects/:id` 响应不再需要额外 raw override 字段，ProjectPage 也能正确预填编辑表单。
- 刷新或重新进入项目后，proof card 与编辑态看到的是同一组 provider 选择。
- 前端类型、测试 mock、实际接口合同三者保持一致，不再由测试掩盖合同缺口。
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-provider-contracts/07-03-SUMMARY.md`
</output>
