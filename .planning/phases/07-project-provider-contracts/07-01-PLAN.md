---
phase: 07-project-provider-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/project.py
  - backend/app/schemas/project.py
  - backend/app/api/v1/routes/projects.py
  - backend/alembic/versions/0003_phase7_project_provider_contracts.py
  - backend/tests/factories.py
  - backend/tests/test_api/test_projects.py
  - backend/tests/test_migrations.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
must_haves:
  truths:
    - "创作者可以在同一项目上分别保存 text、image、video provider 选择。"
    - "项目重新加载后仍返回上次保存的 provider 选择。"
    - "每个 modality 都能明确显示当前是项目覆盖还是默认继承。"
  artifacts:
    - path: "backend/app/models/project.py"
      provides: "Project 级 provider override 字段"
    - path: "backend/app/schemas/project.py"
      provides: "项目 provider 读写 DTO 与 source/effective contract"
    - path: "backend/app/api/v1/routes/projects.py"
      provides: "create/get/update project provider persistence and reload surface"
    - path: "backend/alembic/versions/0003_phase7_project_provider_contracts.py"
      provides: "project provider override schema migration"
  key_links:
    - from: "backend/app/api/v1/routes/projects.py"
      to: "backend/app/models/project.py"
      via: "POST/GET/PUT/PATCH payload mapping"
      pattern: "text_provider_override|image_provider_override|video_provider_override"
    - from: "backend/app/schemas/project.py"
      to: "backend/app/api/v1/routes/projects.py"
      via: "ProjectRead model serialization"
      pattern: "provider.*source|effective"
---

<objective>
让项目级 provider 选择先成为稳定后端合同：可存、可取、可区分来源。

Purpose: 先锁定项目 CRUD 合同，给后续 Phase 08/09 的校验与 snapshot 语义提供可信输入边界。
Output: Project model/migration、provider DTO、项目 CRUD 回显与回读测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@backend/app/models/project.py
@backend/app/schemas/project.py
@backend/app/api/v1/routes/projects.py
@backend/tests/test_api/test_projects.py
@backend/tests/test_migrations.py

<interfaces>
From backend/app/models/project.py:
```python
class Project(SQLModel, table=True):
    id: Optional[int]
    title: str
    story: Optional[str]
    style: str
    summary: Optional[str]
    video_url: Optional[str]
    status: str
    created_at: datetime
    updated_at: datetime
```

From backend/app/schemas/project.py:
```python
class ProjectCreate(BaseModel):
    title: str
    story: str | None
    style: str | None
    status: str | None

class ProjectUpdate(BaseModel):
    title: str | None
    story: str | None
    style: str | None
    status: str | None

class ProjectRead(BaseModel):
    id: int
    title: str
    story: str | None
    style: str | None
    summary: str | None
    video_url: str | None
    status: str
    created_at: datetime
    updated_at: datetime
```

Target contract to create in this plan:
```python
class ProjectProviderEntry(BaseModel):
    override_key: str | None
    effective_key: str
    source: Literal["project", "default"]

class ProjectProviderSettingsRead(BaseModel):
    text: ProjectProviderEntry
    image: ProjectProviderEntry
    video: ProjectProviderEntry
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁定项目 provider CRUD 与迁移回归测试</name>
  <files>backend/tests/test_api/test_projects.py, backend/tests/test_migrations.py, backend/tests/factories.py</files>
  <behavior>
    - Test 1: POST /api/v1/projects 接受 text/image/video override，并返回每个 modality 的 override_key、effective_key、source。
    - Test 2: GET/PUT/PATCH round-trip 后 provider 选择保持一致；null override 回读为 source="default"。
    - Test 3: Alembic head 数据库包含 project 的三个 override 列。
  </behavior>
  <action>先扩展现有项目 API 与迁移测试，锁定 Phase 07 的最小合同。工厂 helper 允许创建带 override 的项目 fixture。把可接受值限制在当前仓库已支持的 provider key：text 仅 `anthropic|openai`，image 仅 `openai`，video 仅 `openai|doubao`，避免任意字符串持久化破坏 PROV-03 的可信来源显示。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_api/test_projects.py backend/tests/test_migrations.py -q</automated>
  </verify>
  <done>测试先失败并准确描述 provider persistence/source contract 与迁移预期。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 Project provider 合同、序列化与数据库迁移</name>
  <files>backend/app/models/project.py, backend/app/schemas/project.py, backend/app/api/v1/routes/projects.py, backend/alembic/versions/0003_phase7_project_provider_contracts.py</files>
  <action>在 `Project` 上新增可空的 `text_provider_override`、`image_provider_override`、`video_provider_override` 三列，并新增 Alembic migration。扩展 project schemas：create/update 可写 override，read 返回按 modality 分组的 provider settings 结构，字段至少包含 `override_key`、`effective_key`、`source`。在 projects route 的 create/get/update/patch 里统一构造这个读模型：显式 override → `source="project"`；空值继承当前默认 → `source="default"`。默认值使用现有系统 truth：text=`anthropic`、image=`openai`、video=`openai`；不要在本计划里修改 generation/runtime/factory 逻辑，Phase 07 只交付项目合同与来源可见性。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_api/test_projects.py backend/tests/test_migrations.py -q</automated>
  </verify>
  <done>Project CRUD 已持久化三类 provider override，reload 后值不丢失，返回体能区分项目覆盖 vs 默认继承，迁移测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→project CRUD API | 不可信输入进入 provider 选择持久化边界 |
| API→database | provider override/source contract 被长期存储并供后续运行消费 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | `backend/app/api/v1/routes/projects.py` | mitigate | 在 schema 层把 override 限制为当前支持的 provider key，拒绝任意字符串写库。 |
| T-07-02 | I | `ProjectRead.provider_settings` | mitigate | 返回 provider key/source/effective 信息，不返回 API key、base URL 或其他 secret。 |
| T-07-03 | R | `backend/alembic/versions/0003_phase7_project_provider_contracts.py` | mitigate | 用 Alembic migration + migration test 固定 schema 变更，避免环境漂移导致合同不可追溯。 |
| T-07-04 | D | project CRUD | accept | 本计划未引入外部网络调用，风险局限于常规数据库读写；保持现有项目级接口限流策略即可。 |
</threat_model>

<verification>
- `uv run --project backend pytest backend/tests/test_api/test_projects.py backend/tests/test_migrations.py -q`
- 确认 `ProjectRead` 返回的三个 modality 都带 `override_key/effective_key/source`。
</verification>

<success_criteria>
- 项目 API 可以创建、更新、读取 text/image/video provider override。
- 同一项目刷新或重新获取后，provider 值与保存时一致。
- API 明确标记每个 modality 当前来自项目覆盖还是默认继承。
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-provider-contracts/07-01-SUMMARY.md`
</output>
