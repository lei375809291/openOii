---
mode: quick-full
plan_id: 260418-o51
title: 核心业务测试覆盖补强与完整测试执行
autonomous: true
files_modified:
  - backend/tests/test_api/test_generation.py
  - backend/tests/test_api/test_projects.py
  - backend/tests/test_orchestration/test_phase2_recovery.py
  - frontend/app/pages/ProjectPage.test.tsx
  - frontend/app/hooks/useWebSocket.test.ts
  - .planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md
must_haves:
  truths:
    - 本 quick task 所说的“主要业务功能”限定为当前仓库 creator 主链路：项目 provider 选择读取/展示、fresh generate、invalid precheck 阻断、resume 恢复、feedback 触发的新 run/rerun、项目页 run proof surface、WebSocket snapshot 同步。
    - provider portability proof 主链路有自动化回归覆盖：fresh generate 冻结 snapshot、invalid precheck 阻断、resume 复用 run snapshot。
    - 项目页 / WebSocket proof surface 有自动化回归覆盖：前端只展示 selected / resolved / source，并保留最近一次 run snapshot 作为页面证据。
    - 完整后端与前端自动化测试被实际执行，且结果被记录成可审计文件。
  artifacts:
    - path: backend/tests/test_api/test_generation.py
      provides: generate 主链路与 precheck/snapshot 回归
    - path: backend/tests/test_api/test_projects.py
      provides: 项目页 provider proof payload 回归
    - path: backend/tests/test_orchestration/test_phase2_recovery.py
      provides: resume 使用 run snapshot 而非项目当前 provider 的回归
    - path: frontend/app/pages/ProjectPage.test.tsx
      provides: 项目页 proof card 渲染与证据保持回归
    - path: frontend/app/hooks/useWebSocket.test.ts
      provides: run_started snapshot 写入 store 与完成后保留回归
    - path: .planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md
      provides: 完整测试执行记录
  key_links:
    - from: backend/app/api/v1/routes/generation.py
      to: backend/tests/test_api/test_generation.py
      via: generate/precheck/provider_snapshot assertions
    - from: backend/app/agents/orchestrator.py
      to: backend/tests/test_orchestration/test_phase2_recovery.py
      via: resume_from_recovery uses run.provider_snapshot
    - from: frontend/app/hooks/useWebSocket.ts
      to: frontend/app/pages/ProjectPage.tsx
      via: currentRunProviderSnapshot store → proof card rendering
    - from: .planning/quick/260418-o51-core-business-test-coverage/260418-o51-PLAN.md
      to: .planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md
      via: Task 3 verification commands and recorded command/output/status evidence
---

<objective>
补强 provider portability proof 核心业务回归测试，并执行完整后端/前端测试，留下可审计执行记录。

Purpose: 把 milestone 已交付的 provider snapshot 语义、项目页证据面、WebSocket 证据传播变成稳定回归护栏，避免后续修改破坏主链路。
Output: 后端/前端测试补强 + 一份完整测试执行记录文件。

本计划中的“主要业务功能”明确限定为当前 v1.1 creator 关键路径：项目 provider 选择读取/展示、fresh generate、invalid precheck、resume、feedback/new run、项目页 proof card、WebSocket snapshot 传播；不扩展到与本 milestone 无关的泛业务面。
</objective>

<context>
@.planning/STATE.md
@backend/app/api/v1/routes/generation.py
@backend/app/agents/orchestrator.py
@backend/tests/test_api/test_generation.py
@backend/tests/test_api/test_projects.py
@backend/tests/test_orchestration/test_phase2_recovery.py
@frontend/app/hooks/useWebSocket.ts
@frontend/app/pages/ProjectPage.tsx
@frontend/app/pages/ProjectPage.test.tsx
@frontend/app/hooks/useWebSocket.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补强 provider portability proof 后端主链路回归</name>
  <files>backend/tests/test_api/test_generation.py, backend/tests/test_api/test_projects.py, backend/tests/test_orchestration/test_phase2_recovery.py</files>
  <action>围绕 provider portability proof 主链路补测试，不新增无关覆盖面。至少覆盖三类事实：① fresh generate 会把 resolver 结果冻结到 `AgentRun.provider_snapshot`，且 invalid precheck 不会创建 run；② project detail 返回的 provider proof 仍直接反映 resolver/runtime 结果，而不是前端或 schema 猜值；③ resume 在项目 override 已变更时仍优先使用 `run.provider_snapshot` 重建 runtime service。优先复用现有 fixture / stub 风格，断言保持 creator-facing 语义（selected/resolved/valid/reason），不要把测试扩成底层实现细节快照。</action>
  <verify>
    <automated>uv run --project backend pytest backend/tests/test_api/test_generation.py backend/tests/test_api/test_projects.py backend/tests/test_orchestration/test_phase2_recovery.py -q</automated>
  </verify>
  <done>上述后端关键业务测试全部通过，且 provider portability proof 的 generate / project detail / resume 三个关键断点都有明确断言。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补强项目页与 WebSocket proof surface 前端回归</name>
  <files>frontend/app/pages/ProjectPage.test.tsx, frontend/app/hooks/useWebSocket.test.ts</files>
  <action>聚焦 creator 可见的 proof surface，而不是扩散到整页 UI。补强测试以证明：① `run_started` 里的 `provider_snapshot` 会写入 store；② 项目页 proof card 优先展示最近一次 run snapshot，而不是回退到项目当前 provider defaults；③ run 完成/失败后证据仍保留，页面仍只展示 selected / resolved / source 这一最小证据面；④ 对 invalid / degraded provider payload 维持稳定渲染与提示语义。不要引入新的 telemetry 展示，也不要把测试写成脆弱的 DOM 全量快照。</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run app/pages/ProjectPage.test.tsx app/hooks/useWebSocket.test.ts</automated>
  </verify>
  <done>项目页 / WebSocket 关键证据面测试全部通过，且能防止“run snapshot 丢失、回退到项目默认 provider、或 proof card 展示超出约定字段”的回归。</done>
</task>

<task type="auto">
  <name>Task 3: 运行完整测试并写入执行记录</name>
  <files>.planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md</files>
  <action>在前两项补强完成后，执行完整自动化测试并把结果落盘到 `260418-o51-TEST-RUN.md`。记录至少包含：每条命令、执行时间、通过/失败状态、测试计数摘要、若出现失败则记录修复后重跑结果。完整测试范围包含后端全量 pytest、前端全量 vitest，以及前端类型检查；若命令输出过长，记录关键摘要并注明原始终端命令。目标是得到一次可复查的“全绿”证据，而不是只写计划性描述。</action>
  <verify>
    <automated>uv run --project backend pytest && pnpm --dir frontend exec vitest run && pnpm --dir frontend exec tsc --noEmit && test -s .planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md</automated>
  </verify>
  <done>完整后端/前端自动化测试全部通过，且 `260418-o51-TEST-RUN.md` 明确记录命令、结果摘要、最终状态与执行时间。</done>
</task>

</tasks>

<verification>
- 先跑 Task 1/2 的 focused suites，确保新增回归断言稳定。
- 再执行全量 `pytest` + `vitest run` + `tsc --noEmit`，把最终结果写入 quick 目录记录文件。
</verification>

<success_criteria>
- provider portability proof 的 fresh run / invalid precheck / resume snapshot reuse 均有稳定自动化覆盖。
- 项目页与 WebSocket 的 proof surface 对 run snapshot 的展示与保留有稳定自动化覆盖。
- 仓库后端与前端完整自动化测试实际执行为绿色，并存在对应书面记录文件。
</success_criteria>

<output>
完成后产出：`.planning/quick/260418-o51-core-business-test-coverage/260418-o51-TEST-RUN.md`
</output>
